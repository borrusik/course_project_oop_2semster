# Курсовий проект: Shooting Game (Стрільба по мішеням)

![Status](https://img.shields.io/badge/Status-Completed-success)
![Language](https://img.shields.io/badge/Language-C%2B%2B%2FCLI-blue)
![Framework](https://img.shields.io/badge/Framework-Windows%20Forms%20(.NET)-purple)

Цей репозиторій містить вихідний код курсової роботи з дисципліни "Об'єктно-орієнтоване програмування" (2 семестр). Проект є інтерактивною грою, написаною на C++ з використанням бібліотеки Windows Forms.

## 📋 Опис проекту

Додаток "Shooting Targets" — це гра на реакцію та влучність. Користувач повинен влучати мишею по мішенях, що випадково з'являються, за обмежений час.

**Ключові особливості реалізації:**
* Використання **GDI+** для кастомного малювання графіки (градієнтний фон, складні фігури мішеней).
* Розділення логіки (`Game`) та представлення (`MyForm`).
* Обробка подій миші та клавіатури.

## 🎮 Ігровий процес та Функціонал

1.  **Генерація мішеней:**
    * На полі з'являються мішені, що складаються з кількох кольорових кілець (Червоний/Білий/Синій).
    * Реалізовано алгоритм перевірки колізій: мішені не накладаються одна на одну.
2.  **Система очок та таймер:**
    * За кожне влучання нараховуються бали.
    * Гра обмежена за часом (30 секунд).
    * Коли час спливає, з'являється напис "Гру завершено!".
3.  **Управління:**
    * **ЛКМ (Ліва кнопка миші)**: Постріл по мішені.
    * **Кнопка "Restart"** або клавіша **"R"**: Перезапуск гри в будь-який момент.
4.  **Інтерфейс:**
    * Адаптивна верстка (кнопка рестарту та поле підлаштовуються під розмір вікна).
    * Візуальні ефекти (напівпрозора панель статистики).

## 🛠 Технологічний стек

* **Мова:** C++/CLI (Managed C++)
* **Середовище розробки:** Visual Studio
* **Фреймворк:** .NET Framework (Windows Forms)
* **Графіка:** System::Drawing (GDI+)

## 🏗 Структура проекту (Архітектура)

Проект побудовано із застосуванням принципів ООП:

* **`Target`** (`Target.h`, `Target.cpp`)
    * Клас, що описує окрему мішень.
    * *Інкапсуляція:* Зберігає координати `x`, `y`, радіус та стан активності.
    * *Методи:* `Draw()` для малювання себе та `Contains()` для перевірки влучання курсору в область мішені.

* **`Game`** (`Game.h`, `Game.cpp`)
    * Клас-менеджер, що керує станом гри.
    * Зберігає список об'єктів `std::vector<Target*>`.
    * Відповідає за таймер, підрахунок очок та респаун (появу) мішеней.

* **`MyForm`** (`MyForm.h`)
    * Головне вікно програми.
    * Обробляє введення користувача (MouseClick, KeyDown).
    * Ініціює перемальовування екрану через `Invalidate()`.

## 💻 Встановлення та запуск

Для запуску проекту необхідна Visual Studio з компонентом **C++/CLI support**.

1.  **Клонуйте репозиторій:**
    ```bash
    git clone [https://github.com/borrusik/course_project_oop_2semster.git](https://github.com/borrusik/course_project_oop_2semster.git)
    ```
2.  **Відкрийте проект:**
    Запустіть файл рішення `.sln` (або відкрийте папку як проект у Visual Studio).
3.  **Налаштування середовища:**
    * Переконайтеся, що в інсталяторі Visual Studio вибрано навантаження "Розробка класичних програм на C++" (Desktop development with C++).
    * В "Окремих компонентах" (Individual components) має стояти галочка на **C++/CLI support for v143 build tools** (або відповідна версія).
4.  **Збірка:**
    Натисніть `Ctrl + Shift + B` або кнопку "Локальний налагоджувач Windows" (Local Windows Debugger).

## 👤 Автор

**borrusik**
* GitHub: [borrusik](https://github.com/borrusik)

---
*Проект виконано в рамках навчального курсу 2025 року.*
